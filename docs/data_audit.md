# Научный анализ базы Russian Soil Observatory

**Версия слоя наблюдений: 4 августа 2026 г. · 106 839 табличных наблюдений из 1 430 публикаций**

> **Abstract.** This report audits a database of soil measurements extracted from the
> full text of 4 521 Russian soil-science publications (966 original-language
> «Почвоведение» articles, including 341 back-issues added from the RCSI archive and
> re-OCR'd on 2026-08-03, plus 3 555 Springer/*Eurasian Soil Science* translations).
> Rebuilding the observation layer with the current header matcher removed 10 220
> confirmed false positives (chemical symbols matched inside unrelated headers),
> recovered the analytical method for 1 731 pH cells lost to LaTeX markup, and
> restored 1 337 cells that a naive rebuild would have destroyed — including the
> anchors of all 147 manually verified measurements. The property catalogue was then
> extended additively to 101 canonical properties, a word-boundary matching bug was
> found and fixed, and the newly digitised «Почвоведение» back-issues added 1 946
> further observations together with a full spatial re-validation. The layer now
> stands at 106 839 observations. The publication year, previously absent for the
> entire translated corpus and for the newly added back-issues, was recovered from
> the Pleiades DOI and validated against Crossref; it is now present for all 4 521
> documents. Two structural limits dominate any downstream use: a unit is formally
> assigned to every value, but only 53.0 % carry a unit confident enough (printed
> in the header or reliably inferred from context) for quantitative comparison —
> the remaining 47.0 % is a default assumption, not a printed fact — and the 790
> reported coordinates are strongly clustered
> (Clark–Evans R = 0.342, ≈ 302 independent localities at a 25 km merge threshold),
> so the point set is not a probability sample of Russian territory.

---

## Резюме

1. **Слой наблюдений был построен из устаревших кандидатов, затем пересобран и
   дважды расширен.** Пересборка по действующему коду извлечения дала 62 805
   наблюдений вместо 70 828 (−22.9% нераспознанных строк). Последующее расширение
   каталога свойств и добавление 341 новой статьи «Почвоведение» из архива RCSI
   довели слой до текущих **106 839** наблюдений.
2. **Удалено 10 220 подтверждённых ложных срабатываний** при пересборке. Главный
   источник — односимвольные химические символы, найденные внутри произвольного
   текста заголовка. «Бор» был вторым по частоте свойством базы; в действительности
   это были горизонт B, плотность сложения (B.d) и изотопные отношения δ¹³C.
3. **Восстановлен аналитический метод pH.** OCR Springer сохраняет LaTeX‑разметку
   (`pH\( _{H_2O} \)`), из‑за чего 1 731 ячейка теряла указание метода и
   деградировала в «pH без указания метода».
4. **Каталог расширен и статьи «Почвоведение» пополнены.** Добавлены заголовки-ионы,
   фракции по Качинскому, валовые оксиды и элементные отношения (всего 101
   свойство); архив RCSI дал 341 новую статью «Почвоведение» с заново
   распознанным OCR, что добавило 1 946 наблюдений и подняло число публикаций,
   давших числовые наблюдения, до 1 430.
5. **Восстановлена временная ось корпуса.** У всех 3 555 переводных публикаций и
   у вновь добавленных статей «Почвоведение» отсутствовал год; он декодирован
   из идентификатора Pleiades DOI или из печатного каталога. Корпус охватывает
   2006–2026 гг., а не 2019–2023, как выглядело раньше. Пропуск
   `publication_year` закрыт полностью (0% из 106 839 записей).
6. **Пространственная выборка сильно кластеризована** (790 уникальных положений,
   Кларк–Эванс 0.342, ≈ 302 независимых площадки) и не является репрезентативной
   для территории России.
7. **Ни одно значение не удалено «по подозрению».** Всё, что нельзя доказать,
   помечено флагом и остаётся в базе вместе со ссылкой на исходную ячейку.

---

## 1. Состав корпуса

| Слой | Объём |
|---|---:|
| Полнотекстовые публикации | 4 521 |
| — «Почвоведение» (русский оригинал) | 966 |
| — Springer / *Eurasian Soil Science* (перевод) | 3 555 |
| Исходных артефактов (страницы, таблицы, тексты) | 39 415 |
| OCR‑таблиц | 13 358 |
| Публикаций, давших числовые наблюдения | 1 430 |
| — из них «Почвоведение» | 201 |
| Табличных наблюдений | 106 839 |
| Канонических свойств | 101 |

Из 966 статей «Почвоведение» 341 добавлены 3 августа 2026 г. из архива RCSI и
распознаны заново (OCR); из них наблюдения удалось извлечь лишь у части — общее
число статей «Почвоведение» с извлечёнными наблюдениями выросло со 140 до 201.

**Оба корпуса — один журнал.** Все DOI переводного корпуса имеют префикс
`10.1134` (Pleiades Publishing), то есть *Eurasian Soil Science* — англоязычная
версия «Почвоведения». Это меняет интерпретацию базы: это не два независимых
источника, а один журнал в двух языковых версиях.

**Метаданные асимметричны и неполны.** У переводного корпуса нет ни одного
заголовка статьи и ни одного списка авторов; у русского корпуса нет ни одного
DOI. Это ограничивает и цитируемость, и возможность связать оригинал с переводом
(см. раздел 9.1).

---

## 2. Пересборка и рост слоя наблюдений

Аудит полноты в прежней сборке сообщал `coverage_gap: 0, ready: true`. Проверка
покрытия — это проверка того, что ни одна строка не потерялась; она ничего не
говорит о том, правильно ли распознано свойство. Прогон сохранённых заголовков
через действующий код извлечения показал расхождение в **22.9%** записей. С тех
пор слой прошёл ещё два этапа роста (расширение каталога свойств и добавление
архива RCSI), описанных в разделе 2.4.

### 2.1. Что дала пересборка

| Операция | Ячеек |
|---|---:|
| Было в прежнем слое | 70 828 |
| Удалено как подтверждённые ложные срабатывания | −10 220 |
| Восстановлено: якоря проверенных вручную измерений | +147 |
| Восстановлено: корректный заголовок, потерянный эвристикой границы шапки | +1 190 |
| Добавлено: заголовки, ставшие распознаваемыми после LaTeX‑нормализации | +860 |
| **Стало (промежуточный слой)** | **62 805** |

Прямолинейный запуск `--replace-existing` разрушил бы ручную кураторскую работу:
он уничтожал якоря 147 проверенных измерений, включая 74 записи обменных Ca²⁺/Mg²⁺
и активности Ca²⁺, добавленные ручной переклассификацией. Поэтому пересборка
сделана сохраняющей: кандидат восстанавливается, если он либо привязан к
верифицированному измерению, либо его напечатанный заголовок действующий код
по‑прежнему считает корректным.

### 2.2. Природа ложных срабатываний

Химический символ из одной‑двух букв опознавался как элемент, даже если он был
частью совсем другого текста:

| Заголовок в таблице | Опознавалось как | Что это на самом деле |
|---|---|---|
| `B.C.` (1 196 ячеек) | бор | обозначение образца |
| `Thickness of horizons, cm A + B` | бор | мощность горизонтов |
| `\( B/C_{6} \), %` | бор | изотопное отношение δ¹³C (значения ≈ −23‰) |
| `Excipitating minerals Ca` | кальций | минералогический состав |
| `Mn/Fe` | железо | отношение, а не концентрация |

Удалено 10 220 таких ячеек, из них 3 635 «бора», 2 401 «кальция», 1 899 «магния».

### 2.3. Восстановление метода pH

`pH\( H_2O \)`, `pH\( _{H_2O} \)`, `pH\( _{water} \)` — один и тот же показатель в
разной LaTeX‑записи. Действующий шаблон требовал `H2O` без разметки и не срабатывал,
поэтому значение уходило в «pH без указания метода». Введена типографская
нормализация заголовка: снимается только LaTeX‑обёртка, ни один токен не
добавляется и не переинтерпретируется. Эффект — ровно 1 731 ячейка, все переходы
только из `ph_unspecified` в `ph_h2o` (1 678) и `ph_kcl` (53), без побочных
изменений.

Корректность разделения подтверждается самими данными: средний pH(KCl) = 5.24 при
pH(H₂O) = 6.51. Разница в 1.27 единицы — общеизвестное соотношение между солевой и
водной вытяжкой, воспроизведённое здесь независимо.

### 2.4. Дальнейший рост слоя (тот же и следующие дни)

После пересборки слой прошёл ещё два независимых этапа роста, слитых здесь в
единую хронологию:

1. **Расширение каталога свойств.** Добавлены заголовки-ионы (`Ca2+`, `Cl-`),
   фракции по Качинскому (`0.05–0.01`, `<0.001`), валовые оксиды (`SiO2`, `TiO2`)
   и элементные отношения (`C:N`). Слой вырос до 106 286 наблюдений и 101
   свойства без потери ни одной курированной записи (режим `--additive`).
2. **Исправление шаблона без границы слова.** Систематическая широтная проверка
   по всем свойствам (не только по флагманским pH/SOC) вскрыла, что
   `total_phosphorus` матчил «Total Porosity», «Total PAHs» и «Total
   precipitation» как фосфор (82% из 1 525 ячеек — ложные). После исправления
   слой составил 104 893 наблюдения.
3. **Архив RCSI (3 августа 2026 г.).** Добавлены 341 статья «Почвоведение» из
   архива RCSI с заново распознанным OCR, извлечены новые табличные наблюдения,
   пересчитана пространственная привязка (валидация координат, геокодирование).
   Слой вырос на 1 946 наблюдений до текущих **106 839**; ровно эти 1 946 записей
   были причиной временного пропуска `publication_year` (см. раздел 6), который
   закрыт тем же прогоном.

Каталог свойств (101 показатель) не менялся на третьем этапе — рост дал только
новые наблюдения по уже существующим свойствам плюс новые публикации и
пространственные точки.

---

## 3. Спектр свойств

101 каноническое свойство в 15 группах. Десять самых массовых:

| Свойство | Наблюдений | Единица доказана |
|---|---:|---:|
| органическое вещество (гумус) | 5 864 | 61.6% |
| pH водной вытяжки | 5 062 | 97.4% |
| pH (экстрагент не указан автором) | 4 860 | 85.8% |
| органический углерод почвы | 4 819 | 67.8% |
| ион магния Mg²⁺ (метод не указан) | 4 035 | 0.7% |
| ион кальция Ca²⁺ (метод не указан) | 3 761 | 6.1% |
| ион натрия Na⁺ (метод не указан) | 3 369 | 2.1% |
| физическая глина | 3 183 | 16.5% |
| железо | 3 178 | 3.2% |
| оксид железа (Fe₂O₃) | 2 665 | 31.9% |

Заголовки-ионы (Mg²⁺, Ca²⁺, Na⁺), добавленные при расширении каталога (раздел
2.4), заняли места в топ‑10 по числу наблюдений, но почти не имеют доказанной
единицы — это ожидаемо для группы «почвенный раствор», где единица обычно % от
ёмкости обмена и указывается только в тексте методов.

Остаточные 2 559 «бора» (свойство `boron`, unit_proven ≈ 0.3%) — заголовки вида
`B` без дополнительного текста. Отличить колонку бора от колонки горизонта B по
одному символу невозможно, поэтому они сохранены с флагом, а не удалены.

**Корпус сильно концентрирован.** Пять публикаций дают более 1 000 наблюдений
каждая (максимум — 4 265), 358 публикаций (25.0% от 1 430, дающих хоть одно
наблюдение) — менее 10, медианная публикация вносит 25 значений при среднем
74.7. Это означает, что любая невзвешенная статистика по всей базе определяется
несколькими крупными таблицами.

---

## 4. Качество данных

Флаги хранятся в отдельном слое `observation_quality_flag` и ничего не удаляют.
Ниже — актуальные проценты по всем 106 839 наблюдениям.

### 4.1. Надёжность распознавания свойства

| Признак | Ячеек | Доля |
|---|---:|---:|
| `phrase` — свойство названо словом или формулой | 86 063 | 80.6% |
| `symbol_clean` — символ стоит в заголовке один | 15 178 | 14.2% |
| `symbol_embedded` — символ внутри более длинного текста | 5 598 | 5.2% |

`symbol_embedded` следует исключать из анализа по умолчанию.

### 4.2. Физическая правдоподобность

| Признак | Ячеек | Доля |
|---|---:|---:|
| `ok` | 101 203 | 94.7% |
| `out_of_physical_range` | 4 928 | 4.6% |
| `negative_content` | 708 | 0.7% |

Проверка диапазона запускается только там, где единица действительно известна;
pH проверяется всегда, поскольку безразмерен. Наиболее затронутые свойства:
`ph_unspecified` (1 321 вне диапазона), `ph_h2o` (966), `available_phosphorus`
(406), `boron` (403 отрицательных), `clay` (228), `organic_matter` (223),
`soil_organic_carbon` (194), `bulk_density` (166).

Список границ правдоподобия расширен в этой ревизии (раздел 4.3.2): после
того как единица стала известна почти для каждого наблюдения, у части
показателей — элементных оксидов, микроэлементов, ионов почвенного раствора —
впервые обнаружились значения, физически невозможные при этой единице, но
раньше остававшиеся незамеченными, потому что сами эти наблюдения были
`missing_unit` и не участвовали в проверке диапазона.

### 4.3. Единицы измерения

**Единицы измерения полностью переработаны в этой ревизии.** Прежний подход
(единица доказана, только если напечатана в заголовке колонки или найдена
слиянием с подписью к таблице) заменён на ручную перепроверку каждого из
106 839 наблюдений по цепочке «заголовок/подпись таблицы → тип показателя →
контекст статьи → единица → проверка диапазона». Результат хранится в новом
слое `observation_unit_inference` — для каждого наблюдения записаны выбранная
единица, метод её определения, уровень уверенности и текстовое доказательство.

| Статус (`normalization_status`) | Ячеек | Доля |
|---|---:|---:|
| `exact` | 99 101 | 92.8% |
| `converted` | 6 954 | 6.5% |
| `incompatible` | 784 | 0.7% |
| `missing_unit` | 0 | 0.0% |

**Формально единица определена для каждого наблюдения без исключения — но
это не значит, что каждая единица одинаково надёжна.** `observation_unit_inference`
несёт собственный признак уверенности, который и определяет, годится ли
наблюдение для количественного сравнения:

| Уверенность | Ячеек | Доля | Метод |
|---|---:|---:|---|
| `high` | 27 120 | 25.4% | единица напечатана в заголовке колонки (`property_header`) |
| `medium` | 29 533 | 27.6% | выведена из контекста статьи, подписи к таблице или соседних колонок (`article_context`, `generic_article_context`, `same_table_context`, `translation_context`) |
| `low` | 50 186 | 47.0% | предположение — в основном каноническая единица показателя по умолчанию, когда более сильных признаков не нашлось (`property_canonical_fallback`, `property_semantics`, `contextual_hypothesis`, `table_composition_hypothesis`, `document_context_hypothesis`, `page_table_context`, `same_article_context`) |

**Для количественного анализа во всех отчётах, построенных на этой ревизии,
используется `confidence IN ('high','medium')` — 56 653 наблюдения (53.0%
слоя).** Уровень `low` в анализ не включается: это единица «по умолчанию»,
а не то, что напечатано в статье, и её использование для сравнения между
публикациями было бы такой же фальсификацией, какой раньше считалась
подстановка канонической единицы взамен `missing_unit`.

#### 4.3.1. Что изменилось по сравнению с прежним подходом

Прежний конвейер оценивал только заголовок своей колонки и подпись к
таблице; всё остальное автоматически становилось `missing_unit` (70.2%
слоя, затем — после слияния с подписями — 53.4%). Новая перепроверка учла
дополнительные источники — раздел Methods статьи, соседние колонки одной
таблицы, парную русско-английскую публикацию — и довела итоговую
недоказанность истинно до нуля. Прямое следствие: доля наблюдений, пригодных
для количественного сравнения, выросла с 27.8% (`exact`+`converted` до этой
ревизии) до 53.0% (`confidence IN ('high','medium')` теперь) — почти вдвое,
хотя основная масса роста пришлась на средний, не самый строгий уровень
доказательности.

#### 4.3.2. Побочный дефект, обнаруженный при перепроверке

Расширение охвата единицами обнажило независимый, ранее незаметный дефект
проверки правдоподобия (`observatory_v1/flag_observation_quality.py`): для
элементных оксидов (Fe₂O₃, Al₂O₃, CaO, MgO, K₂O, Na₂O, SiO₂, TiO₂, MnO, P₂O₅)
не было верхней границы в 100% (это доля от целого, как и другие показатели
гранулометрии, уже проверяемые на диапазон 0–100), и для микро- и
макроэлементов и ионов почвенного раствора не было верхней границы вовсе.
Пока эти наблюдения имели статус `missing_unit`, проверка диапазона их не
касалась; как только `observation_unit_inference` определил для них единицу,
обнаружились значения вроде «оксид железа = 32 978%» и «подвижный фосфор =
30 410 000 мг/кг» — не ошибки перепроверки единиц, а данные из соседней
колонки таблицы, попавшие не туда при исходном извлечении и ранее просто не
проверявшиеся на правдоподобие. Границы добавлены по каждому классу
показателей (100% для оксидов; консервативные, заведомо завышенные потолки
для микроэлементов и ионов — раздел 4.2); затронутые значения помечены
`out_of_physical_range`, а не удалены.

### 4.4. Подмножество, пригодное к количественному анализу

| Фильтр | Ячеек | Доля |
|---|---:|---:|
| Без флагов качества (`unflagged`) | 96 101 | 90.0% |
| Дополнительно с единицей уверенности `high`/`medium` — **готово к анализу** (`analysis_ready`) | **51 626** | **48.3%** |

Разбивка готового подмножества (51 626 ячеек) по категориям:

| Группа | Ячеек |
|---|---:|
| почвенный раствор | 13 428 |
| органическое вещество и углерод | 8 374 |
| кислотно-основные | 6 952 |
| гранулометрический состав | 5 951 |
| микроэлементы | 5 188 |
| геохимические свойства | 4 427 |
| обменный комплекс | 1 980 |
| потенциальные загрязнители | 1 599 |
| макроэлементы | 1 483 |
| физические свойства | 1 466 |
| засоление и минерализация | 606 |
| гидрофизические свойства | 113 |
| элементный и оксидный состав | 33 |

### 4.5. Контроль сумм гранулометрических фракций

Независимый признак качества `particle_fraction_quality` суммирует
распознанные фракции гранулометрического состава по строке таблицы и
сравнивает сумму со 100%:

| Статус | Строк |
|---|---:|
| `within_100` — сумма 100% ± 1 | 937 |
| `near_100` — сумма 100% ± 5 | 128 |
| `deviates_from_100` — сумма вне ±5 | 2 711 |
| `not_comparable` — распознана одна фракция или менее | 207 |

Большинство строк с отклонением — не ошибка извлечения, а таблицы, печатающие
не все гранулометрические фракции (например, только «физическая глина» и
«физический песок», в сумме уже не обязанные давать 100% из-за пропущенных
промежуточных классов). Признак пригоден как дополнительный фильтр качества
для построчного гранулометрического анализа, но не заменяет `value_plausibility`.

### 4.6. Контекст землепользования и антропогенного воздействия

Отдельный слой `document_context_feature` извлекает из полного текста статьи
(не построчно, а на уровне документа) упоминания землепользования и
антропогенного воздействия на изучаемый участок:

| Тип признака | Записей | Частые значения |
|---|---:|---|
| `land_use` | 27 290 | лес (8 376), пашня (4 415), степь (3 993), городская/урбанизированная (3 119), луг (3 083) |
| `impact` | 23 190 | контроль/фон (5 925), горнодобывающее воздействие (3 249), загрязнение (2 870), удобрение (2 325), тяжёлые металлы (2 096) |

Признак документ-уровня: он характеризует статью в целом, а не конкретную
строку таблицы или разрез, и потому пригоден для описания состава корпуса
(например, доли статей о загрязнённых или сельскохозяйственных участках), но
не для строгого построчного стратифицированного сравнения показателей по
угодью без дополнительной построчной привязки.

---

## 5. Пространственная структура

| Показатель | Значение |
|---|---:|
| Координатных записей, сообщённых авторами | 1 092 |
| Уникальных положений | 790 |
| Медианное расстояние до ближайшего соседа | 1.87 км |
| Точек с соседом ближе 1 км | 41.8% |
| Индекс Кларка–Эванса | **0.342** |
| Занятых ячеек 5°×5° | 88 |
| Доля в Европейской России (< 60° в. д.) | 56.3% |

Индекс Кларка–Эванса 0.342 (при 1.0 для случайного размещения) означает сильную
кластеризацию. Иерархическая группировка показывает, сколько на самом деле
независимых местностей стоит за 790 точками:

| Порог объединения | Независимых локалитетов |
|---|---:|
| 1 км | 564 |
| 5 км | 431 |
| 10 км | 385 |
| 25 км | 302 |
| 50 км | 218 |
| 100 км | 133 |

### 5.1. Консолидация координат и уровни привязки

Помимо напечатанной авторской координаты (`reported`), полный текст статьи
может содержать координату в формате градус-минута-секунда (`dms`) или только
название региона, сводимое к его центроиду (`region`). Отдельная процедура
(`document_coordinate_candidate`, 2 872 кандидатные записи, из них 525
использованы) консолидирует все источники координат документа в единую
таблицу `document_spatial_tier`:

| Уровень (`tier`) | Публикаций | Точность |
|---|---:|---|
| `reported` | 444 | напечатанная координата (эталонный слой) |
| `dms` | 278 | пары градус-минута-секунда из полного текста, медианное расхождение с эталоном 0.2 км |
| `region` | 2 123 | центроид названного региона, медианное расхождение с эталоном 174 км |

Три уровня различаются по точности на три порядка и не взаимозаменяемы:
`reported`/`dms` пригодны для задач цифровой картографии, `region` — только
для зональных обобщений (см. «Широтные градиенты» в [рукописи](manuscript_draft.md)).

**Вывод для пользователя.** 790 координат — это не 790 независимых наблюдений
территории, а примерно 300 исследовательских площадок (302 при пороге
объединения 25 км), каждая с несколькими разрезами. Территория России покрыта
88 ячейками 5°×5°; Сибирь и Дальний Восток представлены заметно слабее
Европейской части (56.3% занятых ячеек приходится на территорию западнее 60° в.
д.). Любая пространственная статистика по этому набору описывает географию
российской почвенной науки, а не географию почв России.

---

## 6. Временная структура

Год публикации отсутствовал у всех 3 555 переводных статей и — до обновления
3 августа 2026 г. — у 1 946 наблюдений из новых статей «Почвоведение» (архив
RCSI), что исключало эти записи из любого временного анализа. Идентификатор
статьи Pleiades кодирует год: `10.1134/S1064229` **3 · 13 · 08 ·** `0085` — том
2013 г., выпуск 8, статья 85.

| Способ определения года | Публикаций | Точность по Crossref |
|---|---:|---|
| Напечатан в каталоге («Почвоведение») | 966 | — |
| Декодирован, серия с номером выпуска | 2 807 | 89 из 89 точно |
| Декодирован, непрерывная серия (блок 60+) | 748 | 40 точно, 32 с отклонением ±1 год |

В непрерывной серии закодирован год подачи, и статья может выйти в следующем томе,
поэтому такие годы публикуются как оценка ±1 год, а не как факт. Поле
`document.publication_year` не изменялось: восстановленные значения хранятся
отдельно вместе с уровнем достоверности. После обновления 2026-08-03 год
известен для всех 4 521 документов (966 + 2 807 + 748 = 4 521), пропуск
`publication_year` в слое наблюдений закрыт полностью (0% из 106 839).

Корпус охватывает **2006–2026 гг.** Доля наблюдений по микроэлементам и
загрязнителям (категории `microelement` + `contaminant`) растёт от 10.3% в
2006–2010 гг. до 20.2% в 2020–2025 гг. (посчитано по восстановленному году
публикации для всех 106 839 наблюдений); погодовой ряд колеблется в диапазоне
4.4–33.7% без монотонного тренда внутри периода, но сравнение пятилетних блоков
показывает устойчивое смещение тематики отечественного почвоведения к геохимии
и загрязнению.

---

## 7. Профильная организация

40 835 наблюдений (38.2%) имеют напечатанную глубину (`depth_top_cm`); они
образуют 8 154 профильные серии «публикация × метка строки». Метка
генетического горизонта (`horizon_label_raw`) после отдельного извлечения из
префикса метки строки таблицы, с обязательной проверкой на соседство с
диапазоном глубины и явным списком исключённых ложных срабатываний
(`observation_horizon_evidence`, 19 911 распознанных строк) заполнена у
19 933 наблюдений из 106 839 (18.7%) — этого достаточно для укрупнённого
сравнения по трём основным горизонтам (A/B/C), но
профильный анализ по мелким градациям глубины по‑прежнему опирается в
основном на числовую глубину, а не на название горизонта.

**pH по глубине** (`ph_h2o`+`ph_kcl`+`ph_unspecified`, только правдоподобные
значения, шаг 20 см):

| Глубина, см | n | Средний pH |
|---|---:|---:|
| 0–20 | 1 796 | 6.14 |
| 20–40 | 713 | 6.32 |
| 40–60 | 450 | 6.63 |
| 60–80 | 289 | 6.63 |
| 80–100 | 208 | 7.20 |
| 100–120 | 147 | 7.00 |
| 120–140 | 97 | 7.49 |
| 140–160 | 88 | 7.36 |
| 160–180 | 45 | 8.07 |
| 180–200 | 32 | 8.36 |
| 200–210 | 15 | 7.88 |

**Органический углерод по глубине** (только с доказанной единицей, г/кг, шаг 20 см):

| Глубина, см | n | Средний C<sub>орг</sub> |
|---|---:|---:|
| 0–20 | 525 | 64.9 |
| 20–40 | 201 | 33.2 |
| 40–60 | 127 | 31.0 |
| 60–80 | 100 | 16.3 |
| 80–100 | 67 | 15.2 |
| 100–120 | 49 | 12.7 |
| 120–140 | 40 | 9.4 |
| 140–160 | 30 | 16.2 |
| 160–180 | 22 | 4.2 |
| 180–200 | 17 | 5.4 |

Оба профиля воспроизводят классические закономерности: подкисление верхних
горизонтов на фоне подщелачивания и карбонатонакопления в нижней части профиля;
экспоненциальное убывание органического углерода с глубиной. Это независимое
подтверждение того, что извлечение и нормализация не вносят систематического
искажения в подмножество с доказанной единицей.

---

## 8. Типы почв и классификация WRB 2022

Слой `observation_soil_type` связывает с наблюдением почвенное название,
упомянутое автором, и, где это возможно, соответствующую референтную группу
международной классификации WRB 2022. Связь установлена для 102 543
наблюдений из 106 839 (96.0%): 429 различных авторских формулировок сведены к
281 нормализованному названию и далее — через таблицу соответствий
`soil_type_wrb_mapping` (285 правил) — к WRB-группе для 98 071 наблюдения
(91.8%) из 25 референтных групп. Полный список сопоставлений русских названий
и групп WRB — в `soil_type_wrb_mapping`; каждое правило хранит способ
установления и собственный уровень доверия.

**Связь имеет собственный уровень доказанности, как и пространственная
привязка, и раздельно оценена по двум осям.** Поле `confidence` показывает,
откуда взято название: `high` (17 448 наблюдений) — из заголовка таблицы,
контекста самой таблицы или прямой классификации разреза; `medium` (40 014) —
из явного утверждения автора в тексте или из подписи к отдельной строке;
`low` (45 081) — единственное название, упомянутое в статье как основное,
распространено на все наблюдения статьи без построчного подтверждения,
поэтому в многоплощадочных сравнительных работах может относиться не к
каждому конкретному разрезу; `unresolved` (4296) — статья не содержит
однозначного названия. Поле `wrb_confidence` отдельно оценивает надёжность
перевода русского названия в группу WRB: `high` (42 535) — по прямому
указанию латинского названия WRB автором; `medium` (44 733) — по таблице
соответствий авторского термина; `low` (10 803) и `not_mappable` (8768) —
термин неоднозначен или не имеет однозначного эквивалента в WRB.

**Для количественного анализа пригоден только пересеченный эталонный
уровень** (`confidence = 'high'` И `wrb_confidence` в {`high`, `medium`}):
15 803 наблюдения из 218 публикаций, 15 референтных групп WRB, из которых
пять обеспечены не менее чем 10 публикациями:

| Группа WRB | Наблюдений | Публикаций |
|---|---:|---:|
| Podzol | 6622 | 84 |
| Chernozem | 4092 | 84 |
| Solonchak | 1773 | 21 |
| Solonetz | 1456 | 23 |
| Kastanozem | 456 | 11 |

Сравнение проведено по всем показателям, у которых для хотя бы двух групп
набралось не менее 5 публикаций — расширенное покрытие единицами (раздел 4.3)
добавило в проверку ионы почвенного раствора, физическую глину и крупный
песок, которых раньше не хватало на сравнение. Проверке подошли десять
показателей и 26 попарных сравнений; после поправки Бенджамини–Хохберга по
всем 26 одновременно значимы два — оба по pH водной вытяжки (Podzol против
Chernozem: медиана 5.27 против 7.75, p < 0.001, q = 0.001; Podzol против
Solonchak: 5.27 против 8.20, p = 0.001, q = 0.014). Органическое вещество,
ранее казавшееся значимо различающимся между чернозёмом и подзолом (4.47%
против 1.57%), **при поправке на возросшее число одновременных сравнений
значимости не сохранило** (5.24% против 2.00%; q = 0.064, порог 0.05) —
направление то же самое, но при 26 одновременных проверках такой разницы уже
недостаточно, чтобы статистически отличить её от случайного совпадения.
Органический углерод в г/кг по-прежнему не различается значимо (26.65 против
17.85 г/кг; p = 0.27). Полные результаты — `table_soil_type_census.csv` и
`table_soil_type_comparison.csv`.

---

## 9. Ограничения и риски

1. **Двойной учёт оригинала и перевода — метод сменён, риск сузился.**
   Прежний метод (сопоставление по отпечатку числовых значений таблиц) заменён
   на разбор печатной библиографической ссылки: каждая статья *Eurasian Soil
   Science* печатает точную ссылку на исходную русскоязычную публикацию
   (журнал, год, номер выпуска, страницы); для статей 2019–2023 гг. эта ссылка
   разобрана и сопоставлена с записью в русском корпусе по совпадению года,
   номера и диапазона страниц —
   `observatory_v1/link_springer_translations.py`. Установлено **608 точных
   соответствий** этим способом; ещё 17 пар, для которых печатная ссылка не
   была разобрана однозначно или относятся к статьям архива РЦНИ (2024–2026
   гг.), установлены по отпечатку числовых значений (для архива РЦНИ — по
   значениям, извлечённым прямо из ячеек OCR-таблиц, поскольку эти статьи ещё
   не прошли полную материализацию) и сохраняют статус `candidate`. Всего
   **625 соответствий** (608 `confirmed` + 17 `candidate`) в `document_link`.
   Документы не объединяются — связь только помечает дубликат. Из 201 русской
   статьи «Почвоведение», давшей материализованные табличные наблюдения, 146
   (72.6%) входят в число подтверждённых пар. Печатная ссылка на оригинал
   разобрана только для статей 2019–2023 гг.: статьи архива РЦНИ (2024–2026
   гг.) частично покрыты сопоставлением по отпечатку значений (16 из 341), но
   по большей части подтверждённых переводных пар пока не имеют. Остаточная
   верхняя граница риска двойного учёта — **1.5% массива** (1 568 наблюдений в
   40 статьях «Почвоведение», не сопоставленных ни одним из 625 соответствий),
   заметно уже прежней оценки в 4.6%, поскольку метод по печатной ссылке не
   зависит от совпадения таблиц и годового допуска ±1, на которых строился
   отпечаток.
2. **Горизонты теперь распознаются, но не для всех наблюдений.** Метка
   генетического горизонта после отдельного извлечения из префикса метки
   строки таблицы, с обязательной проверкой на соседство с диапазоном глубины
   (`observation_horizon_evidence`, 19 911 распознанных строк) заполнена у
   19 933 наблюдений из 106 839 (18.7%) — этого достаточно для укрупнённого
   сравнения по горизонтам A/B/C, но не заменяет числовую глубину как
   основное измерение профиля для остальных 81.3% наблюдений.
3. **47.0% значений имеют единицу лишь предположительно** (`confidence =
   'low'` в `observation_unit_inference`, раздел 4.3) — формально единица
   определена для каждого наблюдения без исключения, но для почти половины
   слоя это каноническая единица показателя по умолчанию, а не то, что
   напечатано в статье, и в количественные сравнения она не включается.
   Для количественного анализа пригодны 53.0% слоя (`confidence IN
   ('high','medium')`).
4. **Кластеризованная выборка** (см. раздел 5) исключает выводы уровня страны.
5. **Остаточная неоднозначность односимвольных заголовков** (2 559 «бора» и
   аналогичные) не разрешима автоматически.

---

## 10. Приоритеты дальнейшей работы

1. **Единицы измерения — доля надёжного определения выросла с 27.8% до
   53.0%.** Ручная перепроверка по полной цепочке признаков (раздел 4.3)
   довела формальную недоказанность до нуля, но 47.0% слоя получили единицу
   лишь предположением (`confidence = 'low'`). Дальнейшее уточнение
   контекстных признаков (в первую очередь — раздел Methods статьи) может
   перевести часть этого резерва в средний или высокий уровень уверенности.
2. **Связка оригинал ↔ перевод — сделано для статей 2019–2023 гг., частично
   для архива РЦНИ.** Метод по печатной ссылке Springer даёт 608 подтверждённых
   пар из 625; для архива РЦНИ (2024–2026 гг., 341 статья) отпечаток числовых
   значений по сырым OCR-ячейкам добавил ещё 16 пар статуса `candidate` —
   остальные статьи архива пока не имеют ни подтверждённой, ни кандидатной
   пары; их дальнейшее покрытие требует полной материализации табличных
   наблюдений для этого сегмента (раздел 9, пункт 1).
3. **Разбор односимвольных заголовков по контексту таблицы.** Если соседние
   колонки — элементы, `B` это бор; если соседние строки — горизонты, это горизонт.
4. **Распознавание генетических горизонтов — расширено, но не завершено.**
   Заполнены у 19 933 наблюдений из 106 839 (18.7%); оставшиеся 81.3% либо не
   имеют метки горизонта в исходной таблице, либо префикс метки строки не
   поддаётся однозначному отличению от обычного слова без дополнительного
   текстового анализа (см. раздел 4, `REJECT`-список известных ложных
   срабатываний в `observatory_v1/backfill_horizon_label_from_row_prefix.py`).
5. **Привязка строк к конкретным разрезам** в статьях с несколькими точками
   (наблюдения со статусом `document_multiple_reported_coordinates`).

---

## Воспроизведение

```bash
python3 observatory_v1/extract_table_measurement_candidates.py --db DB --replace-existing
python3 observatory_v1/normalize_table_measurement_candidates.py --db DB
python3 observatory_v1/materialize_full_table_observations.py --db DB
python3 observatory_v1/flag_observation_quality.py --db DB --output docs/data/observation_quality_audit.json
python3 observatory_v1/infer_springer_publication_year.py --db DB --crossref docs/data/doi_metadata.csv
python3 docs/scripts/build_portal.py --db DB --output docs/data
```

Все числа этого отчёта воспроизводятся из `docs/data/aggregates.json`,
`docs/data/full_table_observation_audit.json`, `docs/data/observation_quality_audit.json`,
`docs/data/publication_year_audit.json`, `docs/tables/table_property_census.csv` и
`docs/tables/manuscript_analysis.json`.
